# Integration tests placeholder

This directory will hold cross-crate integration tests once
non-trivial functionality exists (planned: Session 02 onward).

Per-crate unit tests live inside each crate at `crates/<name>/src/`.

Do **not** add empty `#[test]` functions here just to look productive.
See `docs/ai/TEST_MATRIX.md` for the canonical list of capabilities
that need cross-crate coverage.
