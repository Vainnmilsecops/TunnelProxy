# Development scripts

Local developer scripts. Each script must be safe to run from the
workspace root on a developer machine.

Planned scripts (added as the need arises):

- `check.sh` — run `fmt --check`, `clippy -D warnings`, `test`, and
  `build` in one go. Will be added in a future session.
