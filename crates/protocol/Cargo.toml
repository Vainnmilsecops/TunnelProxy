[package]
name = "tunnelproxy-protocol"
description = "Wire protocol types and codecs between TunnelProxy Edge and Agent."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
# Async I/O primitives — codec sits at the I/O boundary.
tokio = { workspace = true, features = ["io-util"] }
tunnelproxy-common = { path = "../common" }

# Derive macros for thiserror.
thiserror = "2"
