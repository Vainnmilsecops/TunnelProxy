[package]
name = "tunnelproxy-agent"
description = "TunnelProxy local agent / CLI runtime."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
tokio = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
tunnelproxy-protocol = { path = "../protocol" }

[dev-dependencies]
# Used only by the in-crate integration tests that drive the edge
# listener and the agent client together.
tunnelproxy-edge = { path = "../edge" }